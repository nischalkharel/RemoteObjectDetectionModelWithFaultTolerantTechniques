# Requirements for Jetson Xavier NX (CUDA 10.2, Python 3.6, JetPack 4.6)
# Install with: pip install -r requirements_xavier.txt

# IMPORTANT: This file lists only the pip-installable packages
# See CLAUDE.md for complete installation instructions including:
#   - PyTorch 1.10.0 (NVIDIA wheel)
#   - Torchvision 0.11.1 (build from source)
#   - Ultralytics 8.0.208 (Python 3.6 compatible version)

# Core numerical and scientific computing
numpy==1.19.5
scipy==1.5.4

# Image Processing
opencv-python==4.5.3.56
Pillow==8.4.0

# Data Processing
pandas==1.1.5
PyYAML

# Visualization
matplotlib==3.3.4
seaborn

# Utilities
requests
tqdm
psutil
py-cpuinfo
importlib-metadata

# Note: These specific versions are tested and confirmed working on Xavier NX
# DO NOT install ultralytics via pip - it requires Python 3.8+
# PyTorch 1.10.0 and torchvision 0.11.1 must be installed separately (see CLAUDE.md)
